# Canonical origin of the deployed site, without a trailing slash.
# Used for metadataBase, canonical URLs, sitemap.xml and robots.txt.
# If unset, the build falls back to https://anilkumar.dev
NEXT_PUBLIC_SITE_URL=https://your-domain.com
